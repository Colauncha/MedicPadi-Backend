{
  "name": "community",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/community/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "webpack-cli build",
        "args": ["--node-env=production"],
        "cwd": "apps/community"
      },
      "configurations": {
        "development": { "args": ["--node-env=development"] },
        "staging": { "args": ["--node-env=staging"] }
      }
    },
    "prune-lockfile": {
      "dependsOn": ["build"],
      "cache": true,
      "executor": "@nx/js:prune-lockfile",
      "outputs": [
        "{workspaceRoot}/dist/apps/community/package.json",
        "{workspaceRoot}/dist/apps/community/package-lock.json"
      ],
      "options": { "buildTarget": "build" }
    },
    "copy-workspace-modules": {
      "dependsOn": ["build"],
      "cache": true,
      "outputs": ["{workspaceRoot}/dist/apps/community/workspace_modules"],
      "executor": "@nx/js:copy-workspace-modules",
      "options": { "buildTarget": "build" }
    },
    "prune": {
      "dependsOn": ["prune-lockfile", "copy-workspace-modules"],
      "executor": "nx:noop"
    },
    "serve": {
      "continuous": true,
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "community:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": { "buildTarget": "community:build:development" },
        "production": { "buildTarget": "community:build:production" },
        "staging": { "buildTarget": "community:build:staging" }
      }
    },
    "docker-build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -f apps/{projectName}/Dockerfile -t colauncha/medicpadi-{projectName}:latest ."
      }
    },
    "migration:generate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "typeorm-ts-node-esm migration:generate apps/community/src/migrations/CommunityMigration -d apps/community/data-source.ts"
      }
    },
    "migration:run": {
      "executor": "nx:run-commands",
      "options": { "command": "typeorm-ts-node-esm migration:run -d apps/community/data-source.ts" }
    },
    "migration:revert": {
      "executor": "nx:run-commands",
      "options": { "command": "typeorm-ts-node-esm migration:revert -d apps/community/data-source.ts" }
    }
  },
  "tags": []
}